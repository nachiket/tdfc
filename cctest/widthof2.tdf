unsigned[widthof(cat(va,vb))] widthof2(param unsigned [4] a, 
	                            input unsigned [a] va,
                                    param unsigned [4] b,  
                                    input unsigned [b] vb)

{
   state only(va,vb):
      widthof2=cat(va,vb);
}	
