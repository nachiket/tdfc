unsigned[8] add(input unsigned[8] a, input unsigned[8] b)
{
	double temp;
	state only(a,b): 
		add=a+b;
		goto not_only;
	
	// add a second nonsense state...
	state not_only():
		temp=a+b;
		goto only;
}

