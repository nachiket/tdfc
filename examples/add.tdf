double add(input double a, input double b)
{
	double temp;
	state only(a,b):
		temp=0;
		if(a>1) {
			temp=1;
		} else if(a>2) {
			temp=2;
		}

		if(temp>3) {
			add=a+b+temp;
//		} else {
//			temp=-1;
		}
}
